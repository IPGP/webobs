WebObs: created by nedit  

This form allows to submit a request on available PROCS, for a selected time span and output options. For each selected PROC, all the parameters can be edited. After few minutes, it will return all graphs and data export and send an e-mail to the user with a link to result page and archive.